import { PageFrame } from "@/components/shared/page-frame";

export default function NewInstitutionApplicationPage() {
  return <PageFrame eyebrow="Bulk Submission" title="Create institution application" description="Bulk upload workflow scaffold ready for backend connection." />;
}
